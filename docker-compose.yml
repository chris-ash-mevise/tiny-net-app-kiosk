services:
  kiosk:
    build:
      context: .
      args:
        KIOSK_USER: kioskuser
    ports:
      - "3389:3389"
      - "2222:22"
    env_file:
      - .env
    volumes:
      - kiosk-home:/home/kioskuser
    restart: unless-stopped

volumes:
  kiosk-home:
